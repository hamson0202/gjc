# PRD — 金進昌實業有限公司（GJC）官方網站

> 文件用途：記錄這個網站專案「為什麼這樣做」「長怎樣」「做到哪」，方便未來回來接續開發時快速回想。
> 最後整理日期：2026-09-05

---

## 1. 專案概述

**是什麼**：金進昌實業有限公司（GJC）的形象官網。GJC 自 1994 年成立，主要做輕鋼架、輕隔間、防火建材（板材、天花板、岩綿）的批發與工程。這個網站的目的是「讓潛在客戶認識公司、瀏覽產品、並能留下聯絡資訊詢價」——屬於**行銷型官網**，不是電商，也沒有會員系統或後台管理。

**技術骨架**：
- 框架：Next.js 16.3.4（App Router）
- 語言：TypeScript
- UI：React 19 + Tailwind CSS v4
- 動畫：Framer Motion（頁面轉場、視差捲動效果）
- 圖示：lucide-react
- 沒有資料庫、沒有後端 API、沒有 CMS——內容全部寫死在程式碼裡（見第 3 節）

---

## 2. 重要決策

這些是專案目前呈現出來的技術/內容選擇，記錄「選了什麼」和「為什麼合理」，之後改動前可以先想一下是否還適用。

### 2.1 用 `src/` 目錄存放程式碼
建立專案時選擇把 `app/`、`components/`、`data/` 都放進 `src/`，跟 `package.json`、設定檔分開。純粹是整理習慣，不影響功能（詳見前面對話的說明）。

### 2.2 內容用純資料檔，不接 CMS
產品資料集中放在 `src/data/products.ts`，用一個 `Product` 型別 + 陣列描述 5 項產品（輕鋼架、隔間骨料、隔間板材、天花板、岩綿）。首頁的視差展示（`ProductsShowcase.tsx`）跟產品頁（`products/page.tsx`）**共用同一份資料**，只是呈現方式不同。
**為什麼**：目前產品項目少、不常變動，寫死資料檔比接 CMS 簡單很多、也不用維護後台。**如果之後產品要常態更新或想讓非工程人員自己改內容**，才需要考慮換成 CMS（如 Sanity、或簡單的 JSON + 表單後台）。

### 2.3 聯絡表單目前只是「前端展示用」，還沒真的送出資料
`src/components/ContactForm.tsx` 的 `handleSubmit` 只有 `e.preventDefault()` 加上把畫面切到「感謝您的詢問」的成功畫面，**並沒有把表單資料送到任何地方**（沒有 API route、沒有寄信、沒有存資料庫）。
**這是目前最大的功能缺口**——訪客填完表單以為送出去了，但公司端完全收不到。

### 2.4 聯絡資訊尚未補齊
電話與電子信箱目前都是佔位字串「電話待補」「信箱待補」，出現在 `contact/page.tsx` 和 `Footer.tsx` 兩處。地址已經有真實資料（臺中市龍井區藝術街72巷5號1樓）。

### 2.5 視覺與互動風格
- 主色調：深藍（`primary-dark #0f1f4d`／`primary #1a3d8f`）+ 桃紅強調色（`accent #e0177f`），走穩重、專業建材業形象。
- 首頁產品區用 Framer Motion 做「視差捲動」效果（`ProductsShowcase.tsx`），每個產品全螢幕滿版圖＋文字卡片。
- 每次切換頁面會有一個「logo 由灰轉彩色填色」的過場動畫（`PageTransition.tsx`），刻意做出品牌感，但也讓每次切頁多了約 0.8 秒等待時間——這是特意的設計選擇，需注意是否有使用者覺得太慢。

### 2.6 CSR（企業社會責任）頁面的內容選擇
CSR 頁面內容是「長期贊助臺中在地青年演員陳鼎中，見證他拿下金鐘獎」的故事，而非常見的環保/公益捐款類 CSR 內容。頁面內註明「人物照片來源：ETtoday新聞雲，資料整理自公開報導與維基百科」——**這些是引用自公開媒體的第三方素材，非公司原創**，如果之後要正式上線，建議跟法務/當事人確認素材授權是否沒問題。

---

## 3. 網站架構

### 3.1 頁面（Routes）
App Router 慣例：`src/app/<路徑>/page.tsx` 對應網址 `/<路徑>`。

| 路徑 | 檔案 | 內容 |
|---|---|---|
| `/` | `src/app/page.tsx` | 首頁：Hero、產品視差展示、優勢（Why GJC）、廠房實力、團隊介紹、CTA |
| `/products` | `src/app/products/page.tsx` | 產品系列詳細介紹（5 項產品逐一列出特色） |
| `/about` | `src/app/about/page.tsx` | 關於我們：公司基本資料、營業項目、發展歷程時間軸 |
| `/csr` | `src/app/csr/page.tsx` | 企業社會責任：贊助演員陳鼎中的故事 |
| `/contact` | `src/app/contact/page.tsx` | 聯絡我們：公司資訊、Google 地圖、詢問表單 |

所有頁面共用 `src/app/layout.tsx` 這個「外框」：固定在頂部的 `Header`（含手機版漢堡選單）、頁面切換動畫 `PageTransition`、底部的 `Footer`。

### 3.2 元件（`src/components/`）

| 元件 | 用途 |
|---|---|
| `Header.tsx` | 頂部導覽列，含 5 個主選單連結＋「免費諮詢」按鈕；有手機版摺疊選單；用 `usePathname` 標示目前所在頁面 |
| `Footer.tsx` | 底部頁尾：公司簡介、產品連結、網站導覽、聯絡資訊 |
| `ProductsShowcase.tsx` | 首頁用的全螢幕視差捲動產品展示（用 Framer Motion 的 `useScroll`/`useTransform`） |
| `ContactForm.tsx` | 聯絡表單 UI（姓名、電話、Email、需求說明）——**目前不會真的送出資料，見 2.3** |
| `PageTransition.tsx` | 換頁時的 logo 填色轉場動畫 |

### 3.3 資料（`src/data/`）
`products.ts`：定義 `Product` 型別（`slug`、`name`、`shortName`、`tagline`、`description`、`features`）與 5 筆產品資料。這是全站唯一的「資料層」，首頁展示、產品頁、頁尾產品連結都從這裡讀取，**改產品內容只需要改這一個檔案**。

### 3.4 靜態資源（`public/images/`）
分類清楚：`factory/`（廠房照）、`products/`（5 項產品各一張圖）、`team/`（員工照）、`csr/`（陳鼎中相關照片）、`logo.png`/`logo.jpg`。

---

## 4. 目前進度

### 已完成
- ✅ 全站 5 個頁面內容與版型皆已完成（首頁／產品／關於我們／CSR／聯絡我們）
- ✅ RWD 響應式設計（手機/平板/桌面都有對應版型，尤其 Header 選單、Hero 圖片排版）
- ✅ 品牌視覺、動畫效果（頁面轉場、首頁視差捲動）
- ✅ SEO 基本設定：每頁都有 `metadata`（title/description），`layout.tsx` 也有全站預設
- ✅ 已推上 GitHub（`origin/main`，repo: `hamson0202/gjc`），2 次 commit，工作目錄目前乾淨（無未提交變更）

### 尚未完成 / 已知缺口
- ❌ **聯絡表單沒有真的送出資料**（最優先要補的功能）——需要決定要接 Email 服務（如 Resend）、第三方表單服務，還是自己寫 API route 存資料庫
- ❌ **電話、電子信箱尚未填入**（目前是「電話待補」「信箱待補」佔位字串），出現在聯絡頁與頁尾共 2 處
- ⚠️ CSR 頁面使用的第三方媒體照片與資料，授權狀況未確認
- ⚠️ 尚未做正式部署（無 Vercel/其他託管紀錄可查），網域 `gjc.co` 是否已購買/設定待確認
- ⚠️ 目前無任何自動化測試（unit test / e2e test）

### 建議下一步（依優先順序）
1. 補齊真實電話與 Email，兩處都要改（`contact/page.tsx`、`Footer.tsx`）
2. 決定聯絡表單的送出方式並實作（Email 通知最快、成本最低）
3. 確認 CSR 頁面素材授權，或替換成公司自有的公益素材
4. 正式部署上線（Vercel 對 Next.js 支援度最好，設定網域 `gjc.co`）
